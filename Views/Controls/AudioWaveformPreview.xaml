<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DawnCapture.Views.Controls.AudioWaveformPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:DawnCapture.Models">

    <Viewbox Stretch="Uniform">
        <Grid Height="82">
            <ItemsRepeater
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                ItemsSource="{x:Bind Bars, Mode=OneWay}">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Horizontal" Spacing="5" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="models:AudioWaveformBar">
                        <Grid Width="5" Height="82">
                            <Border
                                Width="5"
                                VerticalAlignment="Bottom"
                                CornerRadius="3"
                                Background="{StaticResource DawnAccentBrush}"
                                Height="{x:Bind Height, Mode=OneWay}" />
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </Grid>
    </Viewbox>
</UserControl>
<!-- Copyright (c) 2026 SHAO Liming <lmshao@163.com> -->
<!-- SPDX-License-Identifier: MIT -->

