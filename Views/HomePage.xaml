<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DawnCapture.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel
        Padding="32"
        Spacing="16"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">

        <TextBlock x:Uid="Home_Title" Style="{StaticResource TitleTextBlockStyle}" />

        <TextBlock
            Text="{Binding ElapsedText}"
            Style="{StaticResource TimerTextBlockStyle}" />

        <TextBlock
            Text="{Binding StatusText}"
            HorizontalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

        <StackPanel
            Orientation="Horizontal"
            Spacing="12"
            HorizontalAlignment="Center">

            <Button
                x:Name="DesktopButton"
                x:Uid="Home_Desktop"
                Style="{StaticResource AccentButtonStyle}"
                Click="DesktopButton_Click" />

            <Button
                x:Name="WindowButton"
                x:Uid="Home_Window"
                Click="WindowButton_Click" />

            <Button
                x:Name="RegionButton"
                x:Uid="Home_Region"
                Click="RegionButton_Click" />
        </StackPanel>

        <ListView
            x:Name="WindowList"
            Width="360"
            MaxHeight="240"
            HorizontalAlignment="Center"
            DisplayMemberPath="Title"
            SelectionMode="Single"
            SelectionChanged="WindowList_SelectionChanged"
            Visibility="Collapsed" />

        <StackPanel
            Orientation="Horizontal"
            Spacing="12"
            HorizontalAlignment="Center">

            <Button x:Uid="Home_Pause" Command="{Binding PauseCommand}" />

            <Button x:Uid="Home_Resume" Command="{Binding ResumeCommand}" />

            <Button x:Uid="Home_Stop" Command="{Binding StopCommand}" />
        </StackPanel>
    </StackPanel>
</Page>
